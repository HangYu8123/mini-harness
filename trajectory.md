# Trajectory

Chronological record of updates to mini-harness. Newest entries at the bottom.

| # | Date | Update | Notes |
|---|------|--------|-------|
| 1 | 2026-09-12 | Project created. Wrote `overall_goal.md` (migration goal: HarnessFlow → mini-harness as a lightweight supplement to Claude Code / Codex native harnesses) and this `trajectory.md`. | Directory was empty; no git repo yet. Source pack at `/Users/hangyu/Desktop/HarnessFlow`. |
| 2 | 2026-09-12 | **Phase A — verbatim migration.** Copied HarnessFlow's `token_effective_workflow/` (8 files) + `general_workflow/initialize.instructions.md` (swapped in per request) into `workflow/`; all 18 `_lib/*.md`; `philosophy/`; the 10 agents the fast family and general initialize spawn (devils-advocate, diversifier, online-researcher, focus/broad/free-analyst, implementer, executor, senior-engineer, qa-engineer) + `INDEX.md`; all 9 `request_template/`; the 4 skills they reference (breakdown-pr, code-simplification, code-review-and-quality, claude-native-skills-subagents) + `skills/index.md`; `CLAUDE.md`, `AGENTS.md`, `cli_setup.sh`, `sync_agent_definitions.py`, `.claude/rules/`, `.gitignore`. Regenerated `.claude/agents/` + `.codex/agents/`. | Source = HarnessFlow working tree (commit `aa9f74c` + uncommitted edits, which are the newest adhd_output/wiki changes). Deliberately **not** migrated: `general_workflow/`, `skill_workflow/`, VS Code Copilot pieces (`copilot-instructions.md`, `setup.sh`), the GUI (`harness_gui.*`, `sync_gui_templates.py`), `weekly-update-report` + `write-readme` skills, `skill_workflow_skills.md`, experiments/benchmarks, HarnessFlow's own `repo_info/`. Measured floor of this state (chars/4): main-agent mandatory reads ≈ **23.3k tokens** for a `code` run, ≈ **38.8k** for a `loop` run, + ≈ 3.4k per subagent; whole pack ≈ 128k tokens. |
| 3 | 2026-09-12 | **Phase B — reconstruction into the modern harness style.** Every `*.instructions.md` became a native **skill** `skills/mh-<name>/SKILL.md` (Agent Skills standard; `disable-model-invocation: true` = the old activation gate): `mh-code` · `mh-debug` · `mh-refactor` · `mh-query` · `mh-check` (correctness_check) · `mh-exec` · `mh-pr` · `mh-init` (from general) · `mh-loop`, plus new `mh-wiki` (the every-fifth-run consolidation, extracted from `harness_wiki.md`). Shape per file: frontmatter → **Contract** (inputs · memory · produces · done-when) → numbered **Stages** with evidence and `[PARALLEL]` spawn tables → **Record** with the unchanged log-entry formats → **Red flags** table. `_lib/workflow_contract.md` + 17 satellites collapsed into **`harness/harness.md`** (10 sections, ≈2.2k tok) + 5 satellites (`philosophy`, `run_record`, `repo_map`, `reinitialize`, `loop_control`, `stay_active`). `subagent_contract.md` is gone: **`sync_agents.py`** injects the working rules into every generated definition. Agents: 10 → **9** (`senior-engineer` + `qa-engineer` merged into `verifier`, an isolated reviewer that also runs the `local` review skill); bodies trimmed ~40%. Request templates → skill invocation + inline `dials:`; routers → a 6-line marker-guarded `AGENTS.md` block + `CLAUDE.md` `@AGENTS.md`; `cli_setup.sh` → **`install.sh`** (`.harness/`, `.agents/skills/`, `.claude/skills/` links, native agents, optional `--hooks`); new **`hooks/guard.sh`** (PreToolUse deny of sudo / push / commit / tree-wide rm; JSON deny for Claude Code, exit 2 for Codex). Pack root made self-hosting via symlinks. README rewritten. | **Measured (chars/4):** main-agent floor `code` run **23.3k → 5.9k tok**, `loop` **38.8k → 9.8k**, `init` 7.4k; per-subagent contract reads **3.4k → 0** (rules ride in the ≈0.6k system prompt); whole pack **128k → 55k tok**. **Verified:** all 10 SKILL.md + 9 agent frontmatters parse; every `.harness/*`, `skills/*`, agent, and §-section reference resolves (script check); 9 Codex TOMLs parse; `install.sh` dry-run into a scratch git repo twice (idempotent block, symlinked skills reachable, settings merged); guard denies 6/6 forbidden and allows 5/5 benign commands. **Sources:** arXiv 2609.00006 (eleven-system study; skills 9/11, hooks 8/11, MVH), 2603.25723 (NLAH: contract-first, ≈2k-tok policies, file-backed state most load-bearing), 2604.25850 (structure transfers, prose doesn't), 2606.20631 (verifiable skill contract, Skill-BOM), 2608.27454 (WikiSkill), LangChain 2026-09-08 (verifiers isolated), Anthropic long-running harness; conventions of obra/superpowers 285.8k★, mattpocock/skills 260.5k★, anthropics/skills 176k★, garrytan/gstack ~133k★, addyosmani/agent-skills 93.8k★ (stars via GitHub API 2026-09-12); Claude Code + Codex docs for skills/subagents/hooks/AGENTS.md. **Deliberate drops:** VS Code Copilot support, `multilayer_repos`, `absolutize_pack_paths`, `local_skill_discovery` (native skill listing suffices), `implementer_fallback` (2 lines in §4), `stay_active`'s generation/lock protocol (replaced by the 4-rule bounded-wait text), the online-researcher's paywall-bypass instruction (not carried into a default pack; restore in `agents/online-researcher.agent.md` if wanted). **Unverified:** exact `.codex/hooks.json` key names (noted in `hooks/README.md`). No git repo initialised (not requested). |
| 4 | 2026-09-12 | **Phase C — redesign as a supplementary layer (user direction).** *Kept:* the multi-agent `mh-init` (now with per-run `subagent_model:` / `subagent_effort:` dials), the request-builder GUI (`harness_gui.html` adapted: mode/agent/path controls replaced by a platform switch that sets the invocation token; nine `request_template/*.md` rewritten to `/mini-harness` + header dials), the optional advisors, the memory files, explicit task types. *Changed:* `harness/harness.md` rewritten around **native first** (§2: no harness plan stage; the platform's plan mode, delegation, and permissions are untouched) with the **advisory pass in addition, before execution, non-blocking** (§5, `advisory_budget`); `repo_info/` is **read on need** via its own `README.md` (§4); the update log is **one line per change** (date · task · request · files · functions); every run writes `.harness/exec_traj/<ts>_<task>.md` incl. **harness effect** (helped / hurt / neutral) and `mh-wiki` consolidates every fifth into `repo_info/harness_effect.md` (WikiSkill; executor never reads it); **user choices** go to the trajectory and general ones to `repo_info/preference.md` (§9). *Added:* a single entry skill `mini-harness` (`on · off · status · init · loop · wiki · gui · <request>`) with activation state `.harness/state/active` and a `route.sh` **SessionStart + UserPromptSubmit hook** that injects the protocol reminder while active; **plugin packaging** for both platforms (`.claude-plugin/plugin.json` + marketplace, `.codex-plugin/plugin.json` + `.agents/plugins/marketplace.json`, `hooks/hooks.json` + `hooks-codex.json`); **i-have-adhd vendored verbatim** (MIT, `ayghri/i-have-adhd`) as the default output style; `harness/tasks.md` (per-task advisory focus · skill · memory · record) and `harness/repo_info_README.md` (installed as `repo_info/README.md`). *Removed:* the seven per-task pipeline skills (`mh-code … mh-pr`), `harness/run_record.md`, the old run-record / harness-wiki file names. | **Facts used:** Claude Code plugin skills are always namespaced (`/mini-harness:mini-harness`); bare `/mini-harness` needs the standalone install — both are supported and the GUI's platform switch emits the right token. Codex plugins carry skills + hooks but not custom agents → `install.sh` still installs `.codex/agents/`. Codex hook key names not machine-verified (noted in `hooks/README.md`). **Memory file names changed** (see overall_goal.md). Validation of this phase is recorded in the next row. |
| 5 | 2026-09-12 | **Phase C validation.** Frontmatter of all 8 skills + 9 agent sources parses; every `.harness/*`, skill, agent, and §-section reference resolves; no stale names from the old memory model remain (script check). `claude plugin validate .` passes, also with `--strict` — the validator rejects any `agents` key in `plugin.json`, so worker sources moved to `agent_sources/` and `sync_agents.py` now generates Claude definitions into the plugin-default `agents/` (`.claude/agents` links to it). `install.sh` dry-run into two scratch git repos: idempotent AGENTS.md block, `CLAUDE.md` import, skills linked, 9 + 9 agents, routing hook merged into `.claude/settings.json` and `.codex/hooks.json`, `--guard` merges the safety guard. `route.sh`: silent when inactive; injects the reminder on `UserPromptSubmit` and `SessionStart` when `.harness/state/active` exists (also from a subdirectory); silent for `/mini-harness …` prompts; works without `jq`. GUI (`harness_gui.html` served over http, driven through real DOM events): live-sync badge on, 9 task tabs, platform switch rewrites line 1 (`/mini-harness` · `/mini-harness:mini-harness` · `$mini-harness`, incl. `… init` / `… loop`), debug-only `reproduce`, loop caps, model/effort dials, field fill-in. Pack-root dogfood links rebuilt (`.harness`, `.agents/skills`, `.claude/skills/*`, `.claude/agents`). | **Floor while active** (chars/4): `harness.md` 2.5k + `tasks.md` 0.85k + entry skill 1k + i-have-adhd 1.8k ≈ **6.2k tokens once per session**, plus a ≈ 70-word hook reminder per prompt; `repo_info/` is no longer on the floor at all. Whole pack incl. the GUI ≈ 78k tokens on disk (the 32 KB template snapshot inside the HTML is most of it). **Unverified:** Codex plugin hook key names; whether Codex adds `UserPromptSubmit` stdout as context (Claude Code does). |
| 6 | 2026-09-12 | **README rewritten in the i-have-adhd shape** with the one-sentence summary "Meet MiHa, a mini-harness that lives in Claude Code and Codex in addition to the original workflow." Structure: use it (4 numbered steps, what you see, dials, other commands, prompt builder) → install (three numbered paths: Claude Code plugin, standalone for both, Codex plugin + `install.sh --no-hooks` for `.codex/agents/`) → what it writes → inside the pack → one `Next:` line. Lists capped at 5, no preamble or closer. MiHa is the friendly name; commands stay `/mini-harness`. | `--no-hooks` is the right flag for plugin users who also run `install.sh`, otherwise the plugin hook and the standalone hook would both inject the reminder. |
